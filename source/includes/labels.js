// Should be synchronized manually with index.html.
// For now, this is a simpler option because we want the input container's height to stay the same
// after everything loads up, and on small screens the height depends on the number of labels.

export const labels = {
  FEMINITIVES: 'feminitives',
  LATIN_PROPER_NOUNS: 'latin-proper-nouns',
  FOREIGN_NAMES: 'foreign-names'
};